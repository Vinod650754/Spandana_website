export type EventStatus = "upcoming" | "ongoing" | "completed";

export type SocialLink = {
  label: string;
  href: string;
};

export type SiteStat = {
  value: number;
  suffix: string;
  label: string;
};
